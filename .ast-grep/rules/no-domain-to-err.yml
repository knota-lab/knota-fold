id: no-domain-to-err
language: Rust
rule:
  pattern: $EXPR.to_err()
paths:
  include:
    - src/controllers/**
    - src/modules/**
message: "Domain 错误的 to_err() 会丢失结构化信息变成 500，请使用 to_response() 或转换为 AppError"
severity: warning
