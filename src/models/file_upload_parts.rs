pub use super::_entities::file_upload_parts::{self, ActiveModel, Column, Entity, Model};

// ActiveModelBehavior is provided by _entities/file_upload_parts.rs (default impl).
// No before_save hook required (created_at uses DB default).
